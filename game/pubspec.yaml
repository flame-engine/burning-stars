name: burning_stars
description: A new Flutter project.
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: 1.0.0-rc4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
